import React from "react";

export const Music = () => (
  <div style={{ minHeight: "100vh" }}>
    <header>
      <p>Music page</p>
      <a>This is where spotify integration is going to be (maybe?)</a>
    </header>
  </div>
);
